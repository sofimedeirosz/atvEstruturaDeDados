# atvEstruturaDeDados
Atividade de Estrutura de Dados sobre Listas

Colaboradores:
- Sofia Medeiros da Fonseca
- Gabriel Moreno
- João Gabriel Ohara
- Maria Eduarda de Souza Silveira

Funções Adicionadas:
//Remover todas ocorrencias e retornar quantos removeu
- public int removerTodas(String elemento);
//Retornar qtd total de elementos válidos (exceto null)
- public int contar();
//Adicionar múltiplo elementos
- public void adicionarVarios(String[] vetor);
//Retorne o elemento armazenado numa determinada posição da lista
- public String obter(int indice);
//Insire um elemento numa posição específica da lista
- public boolean inserir(int posicao, String elemento);
//Remove um elemento numa posição específica da lista
- public String removerPorIndice(int posicao);
 //Remove todos os elementos da lista, deixando-a vazia
- public void limpar();
 //Retorna o índice da última ocorrência de um elemento na lista
- public int ultimoIndiceDe(String elemento);
//Conta quantas vezes um determinado elemento aparece na lista
- public int contarOcorrencias(String elemento);
 //Substitui todas as ocorrências de um elemento antigo por um novo elemento
- public int substituir(String elemento, String novoElemento);

